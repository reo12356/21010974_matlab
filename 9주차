1
x =0:0.05:6*pi;
y =sin(x)+0.5*cos(3*x);
figure;
plot(x,y)
xticks(0:pi:6*pi);
title('y=sin(x)+0.5cos(3x)');
xlabel('x');
ylabel('y');
grid on;
2
x=1:1:50;
y=3.^x;
figure;
subplot(1,2,1);
plot(x,y);
subplot(1,2,2);
semilogy(x,y);
%plot에서는 y가 급격히 커지기 때문에 x값이 작을때 변화가 보이지 않지만 semilogy를
%사용하면 데이터를 지수적으로 볼수있기때문에 좋다
3
score=[72,85,91,66,78,88,95,54,60,73,82,90,69,77,84,92,58,63,79,87];
save('score.dat','score','-ascii');
load_score=load('score.dat');
figure;
subplot(1,2,1);
histogram(load_score,5);
subplot(1,2,2);
histogram(load_score,10);
4
f=@(x)sin(x).*exp(-0.1*x);
figure;
fplot(f,[0,10*pi])
title('f(x)=sin(x)e^{-0.1x}');
xlabel('x');
ylabel('f(x)');
grid on;
5 
x=0:0.1:10;
y1=x.^2;
y2=exp(0.5*x);
figure;
plotyy(x,y1,x,y2);
title('y1,y2');
xlabel('x');
ylabel('y');
grid on;
6
t=0:0.02:12*pi;
x=(1+0.03*t).*cos(t);
y=(1+0.03*t).*sin(t);
z=0.2*t;

figure;
plot3(x,y,z)
title('x,y,z');
xlabel('x(t)');
ylabel('y(t)');
zlabel('z(t)');
grid on;
7
[X,Y]=meshgrid(-4:0.2:4,-4:0.2:4);
Z=X.^2-Y.^2;

figure;
mesh(X,Y,Z)
%이 그래프는 saddle surface가 맞다
%x축 방향으로는 아래로 볼록하고
%$y$축 방향으로는 위로 볼록하다
8
[X,Y]=meshgrid(-2*pi:0.2:2*pi,-2*pi:0.2:2*pi);
Z=sin(X).*cos(Y);

figure;
subplot(1,2,1);
mesh(X,Y,Z);
subplot(1,2,2);
contour(X,Y,Z);
%contour그래프는 z 값이 같은 지점들을 선으로 연결하여 보여줍니다
9%
t=0:0.1:25;
y=exp(-0.15*t).*cos(4*t);
env_up=exp(-0.15*t);
env_down=-exp(-0.15*t);
%a
figure;
plot(t,y);
hold on;
plot(t,env_up);
plot(t,env_down);
hold off;
figure;
semilogy(t,abs(y));
%일만 plotsms y가 t에 따라 진폭이 줄며 진동하는걸 볼수있지만
% 로그스케일은 직선위에놓여 선형적 감소를 볼수있다
10
[X,Y]=meshgrid(-6:0.2:6,-6:0.2:6);
Z=cos(X).*sin(Y).*exp(-0.05*(X.^2+Y.^2));
figure; mesh(X,Y,Z);
figure; contour(X,Y,Z,20);
[max_z,idx]=max(Z(:));
[row,col]=ind2sub(size(Z),idx);
max_x=X(row,col);
max_y=Y(row,col);
fprintf('최대값 z',max_z);
fprintf('위치%f, %f',max_x,max_y);
11
t=[0,1,2,3,4,5,6];
C=[12,8.9,6.6,4.9,3.6,2.7,2.0];
figure;
subplot(1,2,1);
plot(t,C,'o-');
subplot(1,2,2);
semilogy(t,C,'s-');
p=polyfit(t,log(C),1); 
k_e=-p(1);
C0_e=exp(p(2));
fprintf('추정된 k: %f\n',k_e);
fprintf('추정된 C: %f\n',C0_e);
12
n=1:1:30;
T1=n;
T2=n.*log(n);
T3=n.^2;
T4=2.^n;
%a
figure;
subplot(1,2,1);
plot(n,T1,n,T2,n,T3,n,T4);
%b
subplot(1,2,2);
semilogy(n,T1,n,T2,n,T3,n,T4);
%c
%일반 그래프에서는 지수 함수가 너무 빨리 증가해서
%  다른 함수들이 모두 바닥에 붙어 보이지만
%로그 스케일 그래프선으로 표현되어 비교에 더 적절하다
16
t=0:0.1:24;
T=15+10*sin(pi*t/12);

figure;
plot(t,T); hold on;
[maxT,maxIdx]=max(T);
[minT,minIdx]=min(T);
plot(t(maxIdx),maxT,'ro',t(minIdx),minT,'bx');
17
t=0:0.1:20;
v=5*exp(-0.2*t);

figure;
subplot(1,2,1); 
plot(t,v);
subplot(1,2,2); 
semilogy(t,v); 
%일반 그래프에서는 속도가 곡선을 그리며 서서히 0에 수렴하지만
% semilogy 그래프에서는 지수적 감소가 직선으로 나타난다.
18
A=[4,-1,2;1,3,-1;2,-2,5];
b=[7;4;10];
A_sq_mat=A^2
A_sq_ele=A.^2;

d=det(A);

x_inv=inv(A)*b;
x_backslash=A\b;
19
A=[2,-1;3,4];
B=[1,0,-2;3,5,1];
C=[6,-3,4;15,14,-1];

X=A\(C-B);

Check=A*X+B;

% inv(A)를 구하는 과정은 연산량이 많고 오차가 발생하기 쉽지만 
% \ 연산자는 가우스 소거법을 최적화하기 때문에 더 효율적이다
20
X=[3,7,2,5;5,9,4,6;8,12,7,10;10,15,9,13;14,18,12,16];
avg=mean(X);
Xc=X-avg;
S=Xc'*Xc;
% S의 대각원소는 각 변수의 분산을 보여주고
% S의 비대각원소는 두 변수 간의 상관관계을 나타낸다.
